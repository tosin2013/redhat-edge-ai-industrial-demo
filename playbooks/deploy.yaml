- name: oc_get_all
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Create a namespace with token  
    shell: whoami && pwd && id && hostname
    register: command_output

  - debug:
      msg: "{{ command_output }}"